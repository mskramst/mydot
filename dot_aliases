# ------------------------------ aliases -----------------------------

# linux cmds
alias ls='ls -h --color=auto'
alias mkdir='mkdir -p'
alias cp='cp -i'
alias mv='mv -i'
alias used="du -sh"
alias chmox='chmod +x'
alias aptup='sudo apt update'
alias aptupg="sudo apt update && sudo apt upgrade"
alias src='exec zsh -l'     # reload zsh
alias bash='exec bash -l'     # reload bash
alias info="sed -n '2p'"
alias cz="chezmoi"
alias czpush="chezmoi cd && chezmoi add -A . && chezmoi commit -m 'update dotfiles'"
alias nv='nvim'

# setup for syncing dot files to and from multiple computers
alias config='/usr/bin/git --git-dir=$HOME/.cfg --work-tree=$HOME' 

# search
alias gs="goog"   
alias ws="websearch" 

# mods charmbracelet - open ai
alias modsls='mods --list'
alias modsh='mods --show'
alias srcz="source ~/.zshrc"
alias srcb="source ~/.bashrc"

# commonly used directories
alias start='cd $STARTANEW'
alias scripts='cd $SCRIPTS'
alias snippets='cd $SNIPPETS'
alias gith='cd $GHREPOS'
alias gitrv='git remote -v'
alias saved='cd $STARTANEW/saved'
alias notes='cd $STARTANEW/writing/notes'
alias l2c='cd $GHREPOS/learn2code'
alias pi='cd $GHREPOS/power-installer'
alias vnotes='vim $STARTANEW/software/vim.md'
alias vimc='vim ~/.vim/vimrc'
alias cloneins='git clone -c http.sslVerify=false'

# gist related
alias gistc='gh gist create -'
alias newgist='gh gist create $STARTANEW/saved/gist'
alias gistls='gh gist ls'
alias gisthelp='cat $STARTANEW/docs/gists-help.txt'

# editing config files
alias hl='highlight -O ansi'

# git
alias status="git status"
alias gita="git add -A ."
alias gitc="git commit -m"
alias gitac="git add -A . && git commit -m"
alias gitcmt="git add -A . && git commit -m 'updated'"
alias gitp="git push origin main && echo 'pushing main to origin'"

#tmux
alias tlw="tmux list-windows"
alias tls="tmux ls"
alias tnew="tmux new -s"
alias attach="tmux a -t"
alias tkill="tmux kill-server"

#useful
alias ytmp3="yt-dlp -x --audio-format mp3" # mp3
alias yt="ytfzf -L"
alias ytlist="mpv --geometry 480x360 --playlist=current"
alias ytget='yt-dlp'
alias entrgo='ls *.go | entr -r go run main.go'

#ai
alias oll='ollama run llama3.2:latest'
alias ol="ollama run llama3"

# open the last modified file in a directory
alias vlast="vim $STARTANEW/lastcmd"

# save last cli cmd to file
alias savelast='atuin history last -f {command} > $STARTANEW/lastcmd'

# cat the last saved buffer
alias catbuf='cat $STARTANEW/saved/buf'
alias copykey='ssh-copy-id -i ~/.ssh/id_ed25519.pub'

# ----- programming specific aliases (not needed on all systems) -----

#javascript
alias spa='npm create vite@latest'
alias pn='pnpm'
alias pnpmup='pnpm add -g pnpm'
alias pstart='pnpm start'
alias pack='cat package.json'
alias publish='npm publish --access=public'
alias vpack='vim package.json'
alias viteup='pnpx create-vite@latest --template react-ts'
alias nextjs='npx create-next-app@latest'
alias viteapp='pnpm create vite@latest'
alias bat='batcat'
alias podat='podman start -a'

#java/kotlin
alias gr='./gradlew'
alias grt='gradle test'
alias grr='gradle run'
alias gtasks='gradle tasks'
alias jc='javac *.java && java Main'
alias dcd='cd dailycoding'
alias javai='sdk install java 21.0.5-tem'
alias gradlei='sdk install gradle 8.11.1'

#php
alias cpsr='composer'
alias cpsral='composer dump-autoload -o'

#python & vm
alias act='. dev/bin/activate'
alias deact='deactivate'
alias pipi='python3 -m pip install'
alias pipu='pip --upgrade pip'

#golang
alias vm='vim main.go'
alias gor='go run main.go'
